<h1>Listing storage_groups</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Storage array</th>
    <th>Hhba</th>
    <th>Vmhost</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @storage_groups.each do |storage_group| %>
  <tr>
    <td><%= storage_group.name %></td>
    <td><%= storage_group.storage_array_id %></td>
    <td><%= storage_group.hhba_id %></td>
    <td><%= storage_group.vmhost_id %></td>
    <td><%= link_to 'Show', storage_group %></td>
    <td><%= link_to 'Edit', edit_storage_group_path(storage_group) %></td>
    <td><%= link_to 'Destroy', storage_group, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Storage group', new_storage_group_path %>
