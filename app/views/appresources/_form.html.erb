<%= form_for(@appresource) do |f| %>
  <% if @appresource.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@appresource.errors.count, "error") %> prohibited this appresource from being saved:</h2>

      <ul>
      <% @appresource.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :application_id %><br />
    <%= f.number_field :application_id %>
  </div>
  <div class="field">
    <%= f.label :service_id %><br />
    <%= f.number_field :service_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
